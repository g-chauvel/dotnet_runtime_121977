<Project>
  <!-- Intentionally empty. Stops MSBuild from importing any Directory.Build.props found
       higher up the directory tree (e.g. when this harness is copied inside another
       repository such as dotnet/runtime, whose root props pull in the Arcade SDK):
       the harness is a plain standalone app that builds with any .NET SDK. -->
</Project>
